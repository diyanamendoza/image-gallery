import React, { Component } from 'react'

// const keywordArray = images.map(entry => entry.keyword);
// console.log(keywordArray);

export default class Header extends Component {



    render() {
        return (
            <div>
                <h2>Gallery of Creatures with Horns</h2>

            </div>
        )
    }
}
